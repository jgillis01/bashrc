# Miscellaneous aliases
alias ducks='du -cks * | sort -rn | head'

#alias vim='nvim'
alias be='bundle exec'

# Make sure dfc is installed
alias df='dfc'

# Alias git to hub
alias git=hub

# Todo.sh alias
alias todo=todo.sh

alias source_bashrc="source ${HOME}/.bashrc"
